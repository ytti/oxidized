#!/usr/bin/env ruby

begin
  require_relative '../lib/oxidized/cli'
  Oxidized::CLI.new.run
rescue StandardError => e
  warn e
  begin
    debug = Oxidized.config.debug
  rescue StandardError
    debug = true
  end
  raise if debug

  exit 1
end
