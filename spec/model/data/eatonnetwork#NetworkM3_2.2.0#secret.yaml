# Secret-redaction tests for the EatonNetwork model.
# With remove_secret set, cmd :secret deletes these JSON keys; non-secret keys stay.
fail:
  - '"passphrase"'
  - '"proxyUsername"'
  - '"proxyPassword"'
  - '"defaultPassword"'
pass:
  - '"firmwareVersion"'
